# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies
node_modules/
# This project uses bun (bun.lock) as its package manager; package-lock.json
# only ever appears here as a byproduct of a one-off `npm install` used to
# run a real tsc/eslint check against the actual dependency tree — never
# meant to be committed.
package-lock.json

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native
# NOTE: ios/ is committed on purpose — it contains BlackGym's crash
# diagnostics (CrashDiagnostics.mm + AppDelegate hook) and must ship even if
# the build pipeline skips prebuild. android/ stays generated.
android/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files
.env*.local

# typescript
*.tsbuildinfo
.vercel

.rork

.env
rork-eslint.config.js
core
node_modules
.expo
# Metro shim cache
.metro-cache/

ios/